import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.leandro.comprasmercado',
  appName: 'Compras Mercado',
  webDir: 'dist'
};

export default config;
